{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for Water Me windows (overlay / onboard / settings)",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
